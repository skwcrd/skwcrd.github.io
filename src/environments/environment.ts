import { Language } from "./language";

export const environment = {
    prod: false,
    defaultLanguage: Language.thai,
    languages: [
        Language.thai,
        Language.eng,
    ],
    version: "",
}
